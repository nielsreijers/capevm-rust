[package]
name = "preprocessor"
version = "0.1.0"
authors = ["Niels Reijers <nielsreijers@gmail.com>"]
edition = "2021"
license = "MIT OR Apache-2.0"

[[bin]]
name = "preprocessor"

[dependencies]
clap = { version = "4.5.21", features = ["derive"] }
opcodes = { path = "../opcodes" }
zip = "2.2.0"
